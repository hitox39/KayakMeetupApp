import React, { Fragment, ReactFragment } from "react";

export default function Button(props: { component: ReactFragment }) {

    return(
        
        <button className='bg-slate-500 w-24 rounded-md font-medium my-6 mx-4 text-black '></button>
    );
}

 